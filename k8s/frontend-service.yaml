apiVersion: v1
kind: Service
metadata:
  name: learnaloud-frontend
  namespace: learnaloud
spec:
  selector:
    app: learnaloud-frontend
  ports:
  - port: 80
    targetPort: 80
  type: LoadBalancer
